      <%= form_for(current_user, url: relationships_path(@user), method: :post, remote: true) do |f| %>
      <%= hidden_field_tag :following_id, @user.id %>
      <%= f.submit 'follow', class: "btn btn-outline-secondary" %>
      <% end %>